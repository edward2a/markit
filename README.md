# markit
A terminal-based markdown viewer

## Build Dependencies

- **CMake** >= 3.11
- **C++17 compiler** (GCC or Clang)
- **Git** (used by CMake to fetch FTXUI automatically)

FTXUI is pulled in automatically via CMake FetchContent — no manual install required.
